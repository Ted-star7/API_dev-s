$kind: http-request
name: Get Customer
method: GET
url: '{{baseUrl}}/api/v1/customer/:customer'
order: 2000
headers:
  - key: Accept
    value: application/json
pathVariables:
  - key: customer
    value: '{{customer_id}}'
    description: Customer ID
