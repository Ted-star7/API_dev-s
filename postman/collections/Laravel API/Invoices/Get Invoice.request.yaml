$kind: http-request
name: Get Invoice
method: GET
url: '{{baseUrl}}/api/v1/invoices/:invoice'
order: 2000
headers:
  - key: Accept
    value: application/json
pathVariables:
  - key: invoice
    value: '{{invoice_id}}'
    description: Invoice ID
